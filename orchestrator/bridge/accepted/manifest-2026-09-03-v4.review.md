# Bridge review — gemini-strategy manifest v4, 2026-09-03

First manifest drafted WITH the site-inventory seed. It worked partly: Gemini
correctly targeted the two schema gaps the seed named (cruise24.ir zero-schema,
exploreorient /tour/ zero-schema). But the seed had sampled the empty /tour/
INDEX (13 bytes), not a product page — so its "no schema" reading was wrong for
exploreorient, and Gemini inherited the error. Fixed the seed to sample real
product pages; re-review below.

| task | property | verdict | disposition |
|------|----------|---------|-------------|
| `cruise24ir-schema-farsi-faq` | cruise24.ir | **valid — real gap** | routed → Cruise24.ir session as visible-FAQ + FAQPage JSON-LD |
| `exploreorient-schema-touristtrip` | exploreorient.com | **redundant + fabricated** | dropped |
| `boutimar-content-b2b-experience` | boutimar.com | **mis-targeted + redundant** | dropped |

Live findings:
- **exploreorient TouristTrip already present.** `/tour/golden-road-to-samarkand/`
  (a real product) already carries TouristTrip + Offer + TravelAgency schema. The
  empty `/tour/` index is what read as "no schema". Also Gemini invented a trip name
  ("Silk Road Overland Cultural Expedition") — the real Central Asia products are
  golden-road-to-samarkand, almaty-and-the-celestial-mountains, nomad-summer, etc.
- **boutimar B2B already built, wrong page.** Gemini aimed at /experience/ (consumer
  tours). The real B2B page is /travel-agents/ — "Iran DMC & MICE services for tour
  operators", licensing, IATA (6×), contracts (11×), "Register your agency", FAM
  trips. Content exists; the open issue is AI-recall of that page (already the routed
  GEO task), not missing content.
- **cruise24.ir FAQ is the one real task.** Zero schema confirmed; Gemini's visa
  answers are ACCURATE (Persian Gulf/Dubai = easy visa; only AROYA Türkiye+Egypt +
  Seychelles = no-visa). Routed as content+schema (FAQPage needs visible Q&A), with a
  flag to verify the booking-process claim before publishing.

Seed fix committed alongside: exploreorient landmarks now sample a real tour +
destination page instead of the empty section indexes, so the next round sees the
TouristTrip schema that is actually there.

Scorecard across four manifests: v1 fabricated a date, v2 proposed live schema, v3
proposed built content, v4 (seed-aided) landed 1 of 3 clean — improving, and the
live-review gate caught every miss. Keep the gate.
